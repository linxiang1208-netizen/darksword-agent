name: Build Collector Dylib
on:
  workflow_dispatch: {}
  push:
    branches: [master]

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Build collector.dylib for iOS arm64
        run: |
          set -x
          xcodebuild -version
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          echo "SDK: $SDK"
          echo "SDK contents:"
          ls "$SDK/usr/include/sys/" | head -20
          
          echo "=== Compiling ==="
          xcrun -sdk iphoneos clang \
            -arch arm64 \
            -dynamiclib \
            -O2 \
            -fPIC \
            -o collector.dylib \
            src/collector_cf.c \
            -isysroot "$SDK" \
            -miphoneos-version-min=13.0 \
            2>&1 || { echo "COMPILE FAILED"; exit 1; }
          
          echo "=== Verify ==="
          file collector.dylib
          nm -g collector.dylib | grep _process
          ls -la collector.dylib

      - uses: actions/upload-artifact@v4
        with:
          name: collector-dylib
          path: collector.dylib
          retention-days: 30