name: Build Collector Dylib
on:
  workflow_dispatch: {}
  push:
    branches: [master]

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Build collector.dylib for iOS arm64
        run: |
          xcodebuild -version
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          echo "SDK: $SDK"
          
          xcrun -sdk iphoneos clang \
            -arch arm64 \
            -shared \
            -O2 \
            -fPIC \
            -o collector.dylib \
            src/collector_cf.c \
            -isysroot "$SDK" \
            -miphoneos-version-min=13.0
          
          echo "=== Verify ==="
          file collector.dylib
          nm -g collector.dylib | grep _process
          otool -l collector.dylib | grep -A5 '__TEXT'
          ls -la collector.dylib

      - uses: actions/upload-artifact@v4
        with:
          name: collector-dylib
          path: collector.dylib
          retention-days: 30